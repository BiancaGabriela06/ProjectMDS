import React from "react";

// this one will be generated if the page does not exist or there is no element
const EmptyList = () => (
    <div>
        <h1>This is an empty list.</h1>
    </div>
);

export default EmptyList;